<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Sign Up</title>
  <link rel="stylesheet" href="/styles.css" />
  <style>.form { max-width: 460px; }</style>
  </head>
<body>
  <h1>Sign Up</h1>
  <p><button onclick="window.location.href='/'">Home</button> <button onclick="window.location.href='/users/login'">Login</button></p>

  <% if (error) { %>
    <p style="color:#b00"><%= error %></p>
  <% } %>

  <form class="form" action="/users/signup" method="POST">
    <div>
      <label>Username</label><br />
      <input type="text" name="username" value="<%= values?.username || '' %>" required />
    </div>
    <div>
      <label>Email</label><br />
      <input type="email" name="email" value="<%= values?.email || '' %>" required />
    </div>
    <div>
      <label>Password</label><br />
      <input type="password" name="password" required />
    </div>
    <p><button type="submit">Create account</button></p>
  </form>
</body>
</html>
