<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Login</title>
  <link rel="stylesheet" href="/styles.css" />
  <style>.form { max-width: 420px; }</style>
  </head>
<body>
  <h1>Login</h1>
  <p><button onclick="window.location.href='/'">Home</button> <button onclick="window.location.href='/users/signup'">Sign up</button></p>

  <% if (error) { %>
    <p style="color:#b00"><%= error %></p>
  <% } %>

  <form class="form" action="/users/login" method="POST">
    <div>
      <label>Username</label><br />
      <input type="text" name="username" value="<%= values?.username || '' %>" required />
    </div>
    <div>
      <label>Password</label><br />
      <input type="password" name="password" required />
    </div>
    <p><button type="submit">Login</button></p>
  </form>
</body>
</html>
